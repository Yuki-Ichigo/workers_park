works # index page

カンパニーメンバー一覧が見れる



管理者・企業担当は
カンパニーメンバーからユーザーを外せる

<% @works.each do |work| %>
	<%= work.image %>
	<%= work.title %>
	<%= work.introduction %>
	<%= work.company.image %>
	<%= work.company.name %>
<% end %>