works # index page

カンパニーメンバー一覧が見れる

<% flash.each do |key, value| %>
  <%= content_tag(:div, value, class: key) %>
<% end %>

管理者・企業担当は
カンパニーメンバーからユーザーを外せる

<% @works.each do |work| %>
	<%= work.image %>
	<%= work.title %>
	<%= work.introduction %>
	<%= work.company.image %>
	<%= work.company.name %>
<% end %>