works # index page

カンパニーメンバー一覧が見れる


管理者・企業担当は
カンパニーメンバーからユーザーを外せる

<% @works.each do |work| %>
	<%= works.image %>
	<%= works.title %>
	<%= works.introduction %>
	<%= works.company.image %>
	<%= works.company.name %>
<% end %>